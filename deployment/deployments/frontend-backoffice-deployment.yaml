apiVersion: apps/v1
kind: Deployment
metadata:
  name: backoffice-app
  namespace: e-ticket
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backoffice-app
  template:
    metadata:
      labels:
        app: backoffice-app
    spec:
      containers:
        - name: backoffice-app
          image: docker.io/rizkyian78/e-ticketing-backoffice-app:f6e1e2d
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
