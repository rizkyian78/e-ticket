apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout-app
  namespace: e-ticket
spec:
  replicas: 1
  selector:
    matchLabels:
      app: checkout-app
  template:
    metadata:
      labels:
        app: checkout-app
    spec:
      containers:
        - name: checkout-app
          image: docker.io//e-ticketing-checkout-app:e4b9a3a
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
