apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: checkoutpage-tls
  namespace: e-ticket
spec:
  secretName: checkoutpage-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - checkoutpage.site

